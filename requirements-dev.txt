# Development / test dependencies (not needed to run the app).
#   pip install -r requirements-dev.txt
-r requirements.txt
httpx>=0.27        # required by starlette/fastapi TestClient
pytest>=8.0        # optional: `pytest tests/` (or `python tests/run_tests.py`)
